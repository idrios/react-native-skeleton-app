/**
 * @format
 */

import 'react-native';
import React from 'react';
//import App from '../src/App';

import {it} from '@jest/globals';


it('renders correctly', () => {
  expect(1).toBe(1)
  //renderer.create(<App />);
});
